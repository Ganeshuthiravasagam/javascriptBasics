var isLoggedIn = true;
var isEmailVerified = true;
var cardInfo = false;

if (isLoggedIn && isEmailVerified && cardInfo) {
    console.log("You're good to make a purchase");
} else {
    console.log("You're not allowed to do the purchase");
}